<?xml version="1.0" encoding="utf-8"?><testsuites name="pytest tests"><testsuite name="pytest" errors="16" failures="0" skipped="10" tests="34" time="2089.807" timestamp="2026-03-16T00:26:43.160872-03:00" hostname="labgrid-fcefyn"><properties><property name="ENV_CONFIG" value="targets/openwrt_one.yaml" /><property name="TARGETS" value="['main']" /><property name="TARGET_MAIN_REMOTE" value="+" /><property name="IMAGE_ROOT" value="/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tftp/openwrt_one/openwrt-25.12-snapshot-r32750-62d8101e09-mediatek-filogic-openwrt_one-initramfs.itb" /><property name="IMAGE_ROOT_GIT_COMMIT" value="180344fede0400123d3a2effaaf7eee26f2702c8" /></properties><testcase classname="tests.test_apk.TestApk" name="test_apk_procd_installed" time="0.001"><skipped type="pytest.skip" message="LG_FEATURE_APK not defined">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_apk.py:10: LG_FEATURE_APK not defined</skipped></testcase><testcase classname="tests.test_apk.TestApk" name="test_apk_add_ucert" time="0.001"><skipped type="pytest.skip" message="LG_FEATURE_ONLINE not defined">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_apk.py:13: LG_FEATURE_ONLINE not defined</skipped></testcase><testcase classname="tests.test_apk.TestApk" name="test_apk_audit" time="0.001"><skipped type="pytest.skip" message="LG_FEATURE_APK not defined">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_apk.py:23: LG_FEATURE_APK not defined</skipped></testcase><testcase classname="tests.test_base" name="test_shell" time="105.767" /><testcase classname="tests.test_base" name="test_firmware_version" time="0.928"><properties><property name="firmware_version" value="r32750-62d8101e09" /><property name="expected_firmware_version" value="r32750-62d8101e09" /></properties></testcase><testcase classname="tests.test_base" name="test_dropbear_startup" time="1.913" /><testcase classname="tests.test_base" name="test_ssh" time="124.483"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 0

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
&gt;                       raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
E                       labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=0 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-a2h9sso9/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-a2h9sso9/proxy-stderr: error from SSH ProxyCommand: 2026/03/16 00:30:36 socat[115483] E connect(5, AF=2 10.13.200.120:22, 16): No route to host

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:167: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_base" name="test_echo" time="120.659"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 3

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=3 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-2t6z3_hx/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-2t6z3_hx/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_base" name="test_uname" time="123.723"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 0

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
&gt;                       raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
E                       labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=0 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-daxw5n9m/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-daxw5n9m/proxy-stderr: error from SSH ProxyCommand: 2026/03/16 00:34:41 socat[115832] E connect(5, AF=2 10.13.200.120:22, 16): No route to host

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:167: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_base" name="test_ubus_system_board" time="124.183"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 0

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
&gt;                       raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
E                       labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=0 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-jvyzobgy/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-jvyzobgy/proxy-stderr: error from SSH ProxyCommand: 2026/03/16 00:36:45 socat[116032] E connect(5, AF=2 10.13.200.120:22, 16): No route to host

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:167: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_base" name="test_free_memory" time="120.551"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 1

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=1 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-w7p5ux3k/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-w7p5ux3k/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_base" name="test_sysupgrade_backup" time="0.001"><skipped type="pytest.skip" message="Skipping because feature &quot;{'rootfs'}&quot; is not supported">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_base.py:69: Skipping because feature "{'rootfs'}" is not supported</skipped></testcase><testcase classname="tests.test_base" name="test_sysupgrade_backup_u" time="0.001"><skipped type="pytest.skip" message="Skipping because feature &quot;{'rootfs'}&quot; is not supported">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_base.py:81: Skipping because feature "{'rootfs'}" is not supported</skipped></testcase><testcase classname="tests.test_base" name="test_kernel_errors" time="124.204"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 0

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
&gt;                       raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
E                       labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=0 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-42uh5ck7/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-42uh5ck7/proxy-stderr: error from SSH ProxyCommand: 2026/03/16 00:40:50 socat[116419] E connect(5, AF=2 10.13.200.120:22, 16): No route to host

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:167: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_lan" name="test_lan_wait_for_link_ready" time="0.957" /><testcase classname="tests.test_lan" name="test_lan_wait_for_network" time="1.149" /><testcase classname="tests.test_lan" name="test_lan_interface_address" time="0.867" /><testcase classname="tests.test_lan" name="test_lan_interface_has_neighbor" time="2.831" /><testcase classname="tests.test_network_security" name="test_ssh_supported_algorithms" time="124.151"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 0

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
&gt;                       raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
E                       labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=0 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-bo4sdyem/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-bo4sdyem/proxy-stderr: error from SSH ProxyCommand: 2026/03/16 00:43:00 socat[116607] E connect(5, AF=2 10.13.200.120:22, 16): No route to host

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:167: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_opkg" name="test_opkg_procd_installed" time="0.001"><skipped type="pytest.skip" message="Skipping because feature &quot;{'opkg'}&quot; is not supported">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_opkg.py:4: Skipping because feature "{'opkg'}" is not supported</skipped></testcase><testcase classname="tests.test_opkg" name="test_opkg_install_ucert" time="0.001"><skipped type="pytest.skip" message="Skipping because features &quot;{'online', 'opkg'}&quot; are not supported">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_opkg.py:9: Skipping because features "{'online', 'opkg'}" are not supported</skipped></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_memory_usage" time="124.187"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 0

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
&gt;                       raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
E                       labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=0 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-5rdo63cj/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-5rdo63cj/proxy-stderr: error from SSH ProxyCommand: 2026/03/16 00:45:04 socat[116792] E connect(5, AF=2 10.13.200.120:22, 16): No route to host

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:167: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_filesystem_usage" time="124.012"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 0

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
&gt;                       raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
E                       labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=0 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-egdv6qb3/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-egdv6qb3/proxy-stderr: error from SSH ProxyCommand: 2026/03/16 00:47:09 socat[116987] E connect(5, AF=2 10.13.200.120:22, 16): No route to host

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:167: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_system_uptime" time="123.996"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 0

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
&gt;                       raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
E                       labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=0 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-ibp9dd7_/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-ibp9dd7_/proxy-stderr: error from SSH ProxyCommand: 2026/03/16 00:49:13 socat[117186] E connect(5, AF=2 10.13.200.120:22, 16): No route to host

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:167: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_temperature_sensors" time="124.051"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 0

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
&gt;                       raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
E                       labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=0 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-lrvy0_9_/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-lrvy0_9_/proxy-stderr: error from SSH ProxyCommand: 2026/03/16 00:51:17 socat[117427] E connect(5, AF=2 10.13.200.120:22, 16): No route to host

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:167: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_process_count" time="120.608"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 1

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=1 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-5ywk2fbl/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-5ywk2fbl/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_entropy_available" time="124.097"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 0

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
&gt;                       raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
E                       labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=0 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-9hj92c_8/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-9hj92c_8/proxy-stderr: error from SSH ProxyCommand: 2026/03/16 00:55:22 socat[117806] E connect(5, AF=2 10.13.200.120:22, 16): No route to host

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:167: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_open_file_descriptors" time="124.020"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 0

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
&gt;                       raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
E                       labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=0 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-1xrllf2i/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-1xrllf2i/proxy-stderr: error from SSH ProxyCommand: 2026/03/16 00:57:26 socat[117990] E connect(5, AF=2 10.13.200.120:22, 16): No route to host

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:167: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_wan" name="test_wan_wait_for_network" time="1.174" /><testcase classname="tests.test_wan" name="test_https_download" time="0.000"><skipped type="pytest.skip" message="Skipping because feature &quot;{'online'}&quot; is not supported">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_wan.py:56: Skipping because feature "{'online'}" is not supported</skipped></testcase><testcase classname="tests.test_wifi" name="test_wifi_wpa3" time="120.508"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 1

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=1 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-j7w7pgm7/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-j7w7pgm7/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_wifi" name="test_wifi_wpa2" time="124.273"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 0

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
&gt;                       raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
E                       labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.120%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=0 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-qasnlcc_/control-10.13.200.120%%vlan200 -p 22 -l root 10.13.200.120%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-xbtci6j9/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.120 22 2&gt;/tmp/lg-ssh-qasnlcc_/proxy-stderr: error from SSH ProxyCommand: 2026/03/16 01:01:32 socat[118372] E connect(5, AF=2 10.13.200.120:22, 16): No route to host

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:167: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bin...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/openwrt_one.yaml')), name=None, state=&lt;Bindi...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_wifi" name="test_wifi_scan" time="0.001"><skipped type="pytest.skip" message="Skipping because feature &quot;{'wifi-neighbors'}&quot; is not supported">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_wifi.py:76: Skipping because feature "{'wifi-neighbors'}" is not supported</skipped></testcase><testcase classname="tests.test_wifi" name="test_wifi_hwsim_sae_mixed" time="0.339"><skipped type="pytest.skip" message="Skipping because feature &quot;{'hwsim'}&quot; is not supported">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_wifi.py:97: Skipping because feature "{'hwsim'}" is not supported</skipped></testcase></testsuite></testsuites>