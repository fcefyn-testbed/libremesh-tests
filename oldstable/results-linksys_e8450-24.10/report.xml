<?xml version="1.0" encoding="utf-8"?><testsuites name="pytest tests"><testsuite name="pytest" errors="16" failures="1" skipped="10" tests="34" time="2066.088" timestamp="2026-03-11T21:13:30.734544-03:00" hostname="labgrid-fcefyn"><properties><property name="ENV_CONFIG" value="targets/linksys_e8450.yaml" /><property name="TARGETS" value="['main']" /><property name="TARGET_MAIN_REMOTE" value="+" /><property name="IMAGE_ROOT" value="/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tftp/linksys_e8450/openwrt-24.10-snapshot-r29135-6f55a70589-mediatek-mt7622-linksys_e8450-initramfs-kernel.bin" /><property name="IMAGE_ROOT_GIT_COMMIT" value="180344fede0400123d3a2effaaf7eee26f2702c8" /></properties><testcase classname="tests.test_apk.TestApk" name="test_apk_procd_installed" time="0.002"><skipped type="pytest.skip" message="LG_FEATURE_APK not defined">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_apk.py:10: LG_FEATURE_APK not defined</skipped></testcase><testcase classname="tests.test_apk.TestApk" name="test_apk_add_ucert" time="0.001"><skipped type="pytest.skip" message="LG_FEATURE_ONLINE not defined">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_apk.py:13: LG_FEATURE_ONLINE not defined</skipped></testcase><testcase classname="tests.test_apk.TestApk" name="test_apk_audit" time="0.000"><skipped type="pytest.skip" message="LG_FEATURE_APK not defined">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_apk.py:23: LG_FEATURE_APK not defined</skipped></testcase><testcase classname="tests.test_base" name="test_shell" time="47.393" /><testcase classname="tests.test_base" name="test_firmware_version" time="1.004"><properties><property name="firmware_version" value="r29135-6f55a70589" /><property name="expected_firmware_version" value="r29135-6f55a70589" /></properties></testcase><testcase classname="tests.test_base" name="test_dropbear_startup" time="1.852" /><testcase classname="tests.test_base" name="test_ssh" time="124.524"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 0

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
&gt;                       raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
E                       labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=0 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-jrhtu2u3/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-jrhtu2u3/proxy-stderr: error from SSH ProxyCommand: 2026/03/11 21:16:25 socat[640287] E connect(5, AF=2 10.13.200.196:22, 16): No route to host

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:167: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_base" name="test_echo" time="124.092"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 0

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
&gt;                       raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
E                       labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=0 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-xou_p7vf/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-xou_p7vf/proxy-stderr: error from SSH ProxyCommand: 2026/03/11 21:18:30 socat[640474] E connect(5, AF=2 10.13.200.196:22, 16): No route to host

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:167: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_base" name="test_uname" time="123.848"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 0

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
&gt;                       raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
E                       labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=0 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-smfraep9/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-smfraep9/proxy-stderr: error from SSH ProxyCommand: 2026/03/11 21:20:34 socat[640674] E connect(5, AF=2 10.13.200.196:22, 16): No route to host

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:167: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_base" name="test_ubus_system_board" time="120.960"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 2

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=2 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-vazns44_/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-vazns44_/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_base" name="test_free_memory" time="120.926"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 2

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=2 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-nj18757f/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-nj18757f/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_base" name="test_sysupgrade_backup" time="0.000"><skipped type="pytest.skip" message="Skipping because feature &quot;{'rootfs'}&quot; is not supported">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_base.py:69: Skipping because feature "{'rootfs'}" is not supported</skipped></testcase><testcase classname="tests.test_base" name="test_sysupgrade_backup_u" time="0.000"><skipped type="pytest.skip" message="Skipping because feature &quot;{'rootfs'}&quot; is not supported">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_base.py:81: Skipping because feature "{'rootfs'}" is not supported</skipped></testcase><testcase classname="tests.test_base" name="test_kernel_errors" time="120.962"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 2

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=2 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-dh03mrlo/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-dh03mrlo/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_lan" name="test_lan_wait_for_link_ready" time="0.935" /><testcase classname="tests.test_lan" name="test_lan_wait_for_network" time="1.124" /><testcase classname="tests.test_lan" name="test_lan_interface_address" time="0.824" /><testcase classname="tests.test_lan" name="test_lan_interface_has_neighbor" time="2.789" /><testcase classname="tests.test_network_security" name="test_ssh_supported_algorithms" time="120.795"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 2

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=2 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-w7czkd1p/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-w7czkd1p/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_opkg" name="test_opkg_procd_installed" time="0.000"><skipped type="pytest.skip" message="Skipping because feature &quot;{'opkg'}&quot; is not supported">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_opkg.py:4: Skipping because feature "{'opkg'}" is not supported</skipped></testcase><testcase classname="tests.test_opkg" name="test_opkg_install_ucert" time="0.000"><skipped type="pytest.skip" message="Skipping because features &quot;{'online', 'opkg'}&quot; are not supported">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_opkg.py:9: Skipping because features "{'online', 'opkg'}" are not supported</skipped></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_memory_usage" time="120.767"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 2

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=2 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-d5wer65p/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-d5wer65p/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_filesystem_usage" time="120.948"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 2

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=2 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-p6pdtilt/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-p6pdtilt/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_system_uptime" time="120.840"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 2

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=2 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-mjnb8sj6/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-mjnb8sj6/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_temperature_sensors" time="120.792"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 2

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=2 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-zer9bx58/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-zer9bx58/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_process_count" time="120.945"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 2

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=2 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-yayx7sba/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-yayx7sba/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_entropy_available" time="120.750"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 2

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=2 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-amy3hukv/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-amy3hukv/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_open_file_descriptors" time="120.648"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 2

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=2 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-0fa564lc/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-0fa564lc/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_wan" name="test_wan_wait_for_network" time="64.668"><failure message="AssertionError: WAN interface did not come up within 60 seconds&#10;assert False">shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)

    @pytest.mark.lg_feature("wan_port")
    def test_wan_wait_for_network(shell_command):
        for i in range(60):
            if ubus_call(shell_command, "network.interface.wan", "status").get(
                "ipv4-address"
            ):
                return
    
&gt;       assert False, "WAN interface did not come up within 60 seconds"
E       AssertionError: WAN interface did not come up within 60 seconds
E       assert False

tests/test_wan.py:53: AssertionError</failure></testcase><testcase classname="tests.test_wan" name="test_https_download" time="0.001"><skipped type="pytest.skip" message="Skipping because feature &quot;{'online'}&quot; is not supported">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_wan.py:56: Skipping because feature "{'online'}" is not supported</skipped></testcase><testcase classname="tests.test_wifi" name="test_wifi_wpa3" time="120.786"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 2

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=2 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-n9tw72bf/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-n9tw72bf/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_wifi" name="test_wifi_wpa2" time="120.897"><error message="failed on setup with &quot;Exception: Timeout while waiting for ssh connection&quot;">self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
&gt;               return self._start_own_master_once(connect_timeout)
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')
timeout = 2

    def _start_own_master_once(self, timeout):
    
        self.tmpdir = tempfile.mkdtemp(prefix='lg-ssh-')
        control = os.path.join(
            self.tmpdir, f'control-{self.networkservice.address}'
        )
    
        args = [self._ssh, "-f", *self.ssh_prefix, "-x", "-o", f"ConnectTimeout={timeout}",
                 "-o", "ControlPersist=300", "-o",
                 "UserKnownHostsFile=/dev/null", "-o", "StrictHostKeyChecking=no",
                 "-o", "ServerAliveInterval=15", "-MN", "-S", control.replace('%', '%%'), "-p",
                 str(self.networkservice.port), "-l", self._get_username(),
                 self.networkservice.address]
    
        # proxy via the exporter if we have an ifname suffix
        address = self.networkservice.address
        if address.count('%') &gt; 1:
            raise ValueError(f"Multiple '%' found in '{address}'.")
        if '%' in address:
            address, ifname = address.split('%', 1)
        else:
            ifname = None
    
        proxy_cmd = proxymanager.get_command(self.networkservice, address, self.networkservice.port, ifname)
        if proxy_cmd:  # only proxy if needed
            args += [
                "-o", f"ProxyCommand={' '.join(proxy_cmd)} 2&gt;{self.tmpdir}/proxy-stderr"
            ]
    
        env = os.environ.copy()
        pass_file = ''
        if self._get_password():
            fd, pass_file = tempfile.mkstemp()
            os.fchmod(fd, stat.S_IRWXU)
            #with openssh&gt;=8.4 SSH_ASKPASS_REQUIRE can be used to force SSH_ASK_PASS
            #openssh&lt;8.4 requires the DISPLAY var and a detached process with start_new_session=True
            env = {'SSH_ASKPASS': pass_file, 'DISPLAY':'', 'SSH_ASKPASS_REQUIRE':'force'}
            with open(fd, 'w') as f:
                f.write("#!/bin/sh\necho " + shlex.quote(self._get_password()))
    
        self.process = subprocess.Popen(args, env=env,
                                        stdout=subprocess.PIPE,
                                        stderr=subprocess.STDOUT,
                                        stdin=subprocess.DEVNULL,
                                        start_new_session=True)
    
        try:
            subprocess_timeout = timeout + 5
            return_value = self.process.wait(timeout=subprocess_timeout)
            if return_value != 0:
                stdout, _ = self.process.communicate(timeout=subprocess_timeout)
                stdout = stdout.split(b"\n")
                for line in stdout:
                    self.logger.warning("ssh: %s", line.rstrip().decode(encoding="utf-8", errors="replace"))
    
                try:
                    with open(f'{self.tmpdir}/proxy-stderr') as proxy_err_fd:
                        proxy_error = proxy_err_fd.read().strip()
                    if proxy_error:
                        raise ExecutionError(
                            f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: error from SSH ProxyCommand: {proxy_error}",  # pylint: disable=line-too-long
                            stdout=stdout,
                        )
                except FileNotFoundError:
                    pass
    
&gt;               raise ExecutionError(
                    f"Failed to connect to {self.networkservice.address} with {' '.join(args)}: return code {return_value}",  # pylint: disable=line-too-long
                    stdout=stdout,
                )
E               labgrid.driver.exception.ExecutionError: Failed to connect to 10.13.200.196%vlan200 with ssh -f -o LogLevel=ERROR -o PasswordAuthentication=no -x -o ConnectTimeout=2 -o ControlPersist=300 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -MN -S /tmp/lg-ssh-pyit6k3y/control-10.13.200.196%%vlan200 -p 22 -l root 10.13.200.196%vlan200 -o ProxyCommand=ssh -x -o LogLevel=ERROR -o PasswordAuthentication=no -o ControlMaster=no -o ControlPath=/tmp/lg-con-y5viwxxm/control-labgrid-fcefyn labgrid-fcefyn -- sudo --non-interactive labgrid-bound-connect vlan200 10.13.200.196 22 2&gt;/tmp/lg-ssh-pyit6k3y/proxy-stderr: return code 255

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:174: ExecutionError

The above exception was the direct cause of the following exception:

shell_command = ShellDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;B...oot', password=None, keyfile='', login_timeout=120, console_ready='', await_login_timeout=15, post_login_settle_time=5)
target = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

    @pytest.fixture
    def ssh_command(shell_command, target):
&gt;       ssh = target.get_driver("SSHDriver")
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/conftest.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.14/site-packages/labgrid/target.py:235: in get_driver
    return self._get_driver(cls, name=name, resource=resource, activate=activate)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.14/site-packages/labgrid/target.py:209: in _get_driver
    self.activate(found[0])
.venv/lib/python3.14/site-packages/labgrid/target.py:472: in activate
    client.on_activate()
.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:72: in on_activate
    self.control = self._start_own_master()
                   ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = SSHDriver(target=Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml')), name=None, state=&lt;Bin...derr_merge=False, connection_timeout=120.0, explicit_sftp_mode=False, explicit_scp_mode=True, username='', password='')

    def _start_own_master(self):
        """Starts a controlmaster connection in a temporary directory."""
    
        timeout = Timeout(self.connection_timeout)
    
        # Retry start of controlmaster, to allow handle failures such as
        # connection refused during target startup
        connect_timeout = round(timeout.remaining)
        while True:
            try:
                return self._start_own_master_once(connect_timeout)
            except ExecutionError as e:
                if timeout.expired:
&gt;                   raise Exception("Timeout while waiting for ssh connection") from e
E                   Exception: Timeout while waiting for ssh connection

.venv/lib/python3.14/site-packages/labgrid/driver/sshdriver.py:104: Exception</error></testcase><testcase classname="tests.test_wifi" name="test_wifi_scan" time="0.000"><skipped type="pytest.skip" message="Skipping because feature &quot;{'wifi-neighbors'}&quot; is not supported">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_wifi.py:76: Skipping because feature "{'wifi-neighbors'}" is not supported</skipped></testcase><testcase classname="tests.test_wifi" name="test_wifi_hwsim_sae_mixed" time="0.331"><skipped type="pytest.skip" message="Skipping because feature &quot;{'hwsim'}&quot; is not supported">/home/laryc/testbed_fcefyn/actions-runner/_work/libremesh-tests/libremesh-tests/tests/test_wifi.py:97: Skipping because feature "{'hwsim'}" is not supported</skipped></testcase></testsuite></testsuites>